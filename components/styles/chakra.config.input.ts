import { ComponentStyleConfig } from '@chakra-ui/react'

export const chakraConfigInputStyle: ComponentStyleConfig = {
  // style object for base or default style

  variants: {
    test: {
        bg: "blue"
    }
  }
}